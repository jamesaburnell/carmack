import React from 'react'

const RegisterStep2 = ({
    _next, 
    _updateInformation
}) => 
    <div>
        Step 2
        <div 
        className="btn btn-default"
        onClick={_next}
        >
            Next
        </div>
    </div>

export default RegisterStep2